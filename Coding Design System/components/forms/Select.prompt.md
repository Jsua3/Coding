Glass dropdown — floating heavily-blurred menu, checkmark on the active option.

```jsx
<Select label="Materia" value={v} onChange={setV} options={[{ value: "bd1", label: "Bases de datos I" }]} />
```
