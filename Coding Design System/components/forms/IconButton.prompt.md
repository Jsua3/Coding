Circular glass icon button — toolbar and chrome actions; always pass `label`.

```jsx
<IconButton label="Buscar" variant="glass"><SearchIcon /></IconButton>
```

- Variants: glass | ghost | accent. `active` marks a toggled state. Press = scale(0.92).
