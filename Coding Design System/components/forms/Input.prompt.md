Glass text field — label above, hint/error below, 4px blue focus halo.

```jsx
<Input label="Correo institucional" placeholder="tu@universidad.edu" hint="Usa tu correo de la universidad" />
```

- `error` overrides `hint` and turns the stroke red. `iconLeft` for search/lock glyphs.
