Segmented glass control — sliding springy thumb; section/view switching.

```jsx
<Tabs value={tab} onChange={setTab} items={[{ id: "temario", label: "Temario" }, { id: "notas", label: "Notas" }]} />
```
