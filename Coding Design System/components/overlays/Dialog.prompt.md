Modal glass sheet over a blurred scrim — confirmations, quiz results.

```jsx
<Dialog open={open} onClose={close} title="¿Enviar respuestas?" footer={<><Button variant="ghost">Cancelar</Button><Button>Enviar</Button></>}>No podras cambiarlas despues.</Dialog>
```
